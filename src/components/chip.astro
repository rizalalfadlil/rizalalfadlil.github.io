---

interface Props {
  name: string;
  icon: string;
  color?: string;
}

const { name, icon, color = "yellow-600" } = Astro.props;

---

<span
  class={`border inline-flex rounded-full p-0.5 px-2 me-2 text-sm gap-2`}
  style={{
    borderColor: color,
    color: color,
    backgroundColor: color + '10',
    
  }}
>
  <img
    width="16"
    src={icon}
  />
  {name}</span
>
