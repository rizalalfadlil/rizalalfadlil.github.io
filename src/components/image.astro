---
const { src, alt, title, url } = Astro.props;
---

<a href={url} class="w-full p-2 space-y-2 inline-block hover:scale-105 transition-all"
>
  <img
    src={src}
    alt={alt}
    class="aspect-video object-cover object-center rounded"
  />
  <p class="text-sm text-center">{title}</p>
</a>
